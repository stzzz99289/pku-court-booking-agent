# Site config for the webapp's "Run Booking" tab (one-off test runs).
# URL, browser tuning, and selectors live in src/booking/site_constants.py.

venue_id: 85
user_data_dir: "./.browser_profile"

headless: true
save_captcha: false
debug: false

# Test runs are immediate (no wait window).
scheduled_mode: false
